---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
  name: probes.check.x-health.io
spec:
  group: check.x-health.io
  names:
    kind: Probe
    listKind: ProbeList
    plural: probes
    singular: probe
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Probe is the Schema for the probes API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ProbeSpec defines the desired state of Probe.
            properties:
              url:
                format: uri
                minLength: 1
                type: string
            required:
            - url
            type: object
          status:
            description: ProbeStatus defines the observed state of Probe.
            properties:
              healthy:
                description: Healthy indicates if the check passed
                type: boolean
              lastCheckTime:
                description: LastCheckTime of the check
                format: date-time
                type: string
              message:
                description: Message from the check (e.g., error details)
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
